# Frontend Folder

This folder is reserved for a separated frontend layout.

The runnable starter currently keeps the Next.js App Router at the repository root in `app/`, which is the default Vercel-friendly layout. If you split the app later, move `app/`, `components/`, `hooks/`, `lib/`, and `styles/` under `frontend/`.
